# OTA update server base URL, e.g. https://updates.example.com/openmower-cm4
# The poller fetches $UPDATE_URL/manifest.json.
# Override per-device in /data/openmower-updater.conf.
UPDATE_URL=""

# Set to 1 to install bundles whose version is older than the running one.
ALLOW_DOWNGRADE=0
